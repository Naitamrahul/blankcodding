# Local backend port
PORT=3001

# Remote Piston API URL (public)
PISTON_REMOTE_URL=https://emkc.org/api/v2/piston/execute

# If true, attempt remote Piston first; if false, execute locally
FORCE_REMOTE=false
